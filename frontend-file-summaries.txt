{
  "frontend/src/pages/NomineeManagementPage.tsx": "Nominee management page for adding/removing nominees with ID proof uploads, updated to import and use `PersistentRelationship` and `PersistentVerificationStatus` instead of the old unprefixed enum names.",
  "frontend/src/pages/AdminLegalVerificationPage.tsx": "Admin-only legal verification module with death certificate upload UI, approve/reject actions with confirmation dialogs, verification status display, and a scrollable audit trail log of all legal events.",
  "frontend/src/pages/ControlledAccessReleasePage.tsx": "Admin-only controlled access release page with multi-step auth flow; fixed by correcting the import of `useGetActivityLogs` (was `useGetAllActivityLogs`) and `useRecordAccessRelease` (now properly exported from useQueries).",
  "frontend/src/pages/AccessReleaseSuccessPage.tsx": "Success confirmation page displayed after a controlled access release, showing a release summary with timestamp, authorization status, blockchain audit confirmation, and navigation back to admin or dashboard.",
  "frontend/src/index.css": "Global stylesheet with OKLCH color system using bare channel values (compatible with the alpha-value Tailwind pattern), global input visibility fixes forcing white text on dark backgrounds, and component classes using plain CSS instead of `@apply` with opacity modifiers.",
  "frontend/src/pages/RegistrationPage.tsx": "Multi-step registration page with profile setup, OTP verification, ID upload, nominee addition, and document upload steps, updated to import and use `PersistentRelationship` instead of the old `Relationship` enum name.",
  "frontend/src/pages/LoginPage.tsx": "Redesigned two-panel login page showing both \"User Login\" and \"Admin Login\" cards simultaneously on initial load; uses `useRef` guards to prevent duplicate OTP step transitions and redirect loops; after OTP verification, waits for both `adminFetched` and `profileFetched` to be true before redirecting to `/admin/dashboard` (admins), `/dashboard` (returning users), or `/register` (new users).",
  "frontend/src/pages/DeathVerificationRequestPage.tsx": "New public page for nominated heirs to submit a death verification request, featuring a multi-section form (deceased info, heir info, document uploads with PDF/JPG/PNG validation up to 10MB), in-app OTP generation and verification flow, and a success confirmation card shown after submission.",
  "frontend/src/pages/AdminDeathVerificationPage.tsx": "Admin-only page displaying a table of all submitted death verification requests with columns for request ID, deceased/heir info, relationship, status badge, timestamp, and Approve/Reject action buttons with confirmation dialogs; includes stats cards and access control guards.",
  "frontend/src/hooks/useQueries.ts": "React Query hooks for all backend operations, now including the previously missing `useRecordAccessRelease` (calls `actor.recordAccessRelease`) and `useSubmitDeathVerificationRequest` (calls `actor.submitDeathVerificationRequest`) mutations, plus all existing hooks for assets, nominees, legal verification, death verification, and admin documents.",
  "frontend/src/App.tsx": "Main app component with TanStack Router; all `/admin/*` routes (dashboard, documents, legal-verification, death-verification, access-release, access-release/success) are now wrapped with `AdminRouteGuard` to enforce authentication and admin-role checks before rendering the page content.",
  "frontend/src/components/Navbar.tsx": "Sticky navigation bar with separate user and admin nav link arrays; admin links and the Admin badge are only rendered when `isAuthenticated && !adminLoading && isAdmin === true` (using a `showAdminLinks` derived boolean), preventing any flicker or incorrect display for guests and regular users; includes full mobile menu support.",
  "frontend/src/pages/LandingPage.tsx": "Full landing page with hero, problem statement, solution overview, features, innovation, and social impact sections; now includes a prominent \"Are You a Nominated Heir?\" CTA banner linking to the death verification request page, plus a second CTA in the final section.",
  "frontend/src/pages/AddAssetPage.tsx": "Page for adding and managing encrypted digital assets on Dead Man's Switch, with platform/username/password/category form fields and a list of stored assets with delete capability.",
  "frontend/src/pages/Dashboard.tsx": "Main user dashboard showing assets, nominees, legal status, and activity logs for Dead Man's Switch; all navigation links corrected to use `/assets/add` instead of the invalid `/add-asset` route.",
  "frontend/tailwind.config.js": "Tailwind configuration with all color tokens updated to use `<alpha-value>` syntax so opacity modifiers (e.g. `border-border/50`, `bg-card/80`) work correctly throughout the app.",
  "frontend/src/pages/AdminDashboardPage.tsx": "Admin-only dashboard page with summary stats and navigation cards; fixed by removing the non-existent `DocumentStatus` import from `../backend` and using string comparison for document status filtering instead.",
  "frontend/src/pages/AdminDocumentReviewPage.tsx": "New admin-only document review page with a table listing all submitted documents (submitter, type, date, status, note), a dialog modal for previewing document images and approving/rejecting with an optional admin note, and full access control enforcement.",
  "frontend/src/components/AdminRouteGuard.tsx": "New route guard component that wraps admin pages; redirects unauthenticated users to `/login`, shows an \"Access Denied\" screen for non-admin authenticated users, displays a loading spinner while the admin check resolves, and renders children only for verified admins."
}